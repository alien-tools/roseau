{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Roseau","text":"<p>Roseau (/\u0281ozo/) is a fast and accurate tool for detecting breaking changes between Java library versions, similar to tools like japicmp or Revapi. Whether you're a library maintainer or upgrading dependencies in your projects, Roseau helps ensure backward compatibility across versions. Roseau analyzes both JAR files and source code, is highly configurable, generates reports in HTML, JSON, and Markdown, and includes a dedicated Maven plug-in.</p> <p>Get the latest release  View on GitHub </p>"},{"location":"#quick-example","title":"Quick Example","text":"<p>When invoked, Roseau extracts the API surface of each version and diffs them to identify breaking changes following a set of well-defined detection rules. Library versions can be passed as local JAR files or source directories, or can be fetched remotely from Maven:</p> <pre><code>$ roseau --diff --v1 library-1.0.0.jar --v2 library-2.0.0.jar\nBreaking changes found: 3 (2 binary-breaking, 2 source-breaking)\n\u2717 com.pkg.A TYPE_REMOVED\n  \u2717 binary-breaking \u2717 source-breaking\n  \u2192 com/pkg/A.java:4\n\u26a0 com.pkg.B.f FIELD_NOW_STATIC\n  \u2717 binary-breaking \u2713 source-compatible\n  \u2192 com/pkg/B.java:18\n\u2605 com.pkg.C TYPE_NEW_ABSTRACT_METHOD [toOverride()]\n  \u2713 binary-compatible \u2717 source-breaking\n  \u2192 com/pkg/C.java:210\n$ roseau --diff --v1 com.example:lib:1.0.0 --v2 /path/to/v2/src/main/java\n[...]\n</code></pre> <p>An example HTML report is available here.</p>"},{"location":"api-surface/","title":"What Roseau considers API","text":"<p>When analyzing a JAR or a source tree, Roseau does not look for breaking changes in every declaration it finds. It first infers the API surface of each version by following Java's accessibility rules and any user-defined exclusion rules. In practice, Roseau follows the accessibility rules defined in the Java Language Specification (e.g., visibility modifiers, module boundaries) to accurately determine which symbols client code can access.</p>"},{"location":"api-surface/#baseline-rules","title":"Baseline Rules","text":"<p>Roseau treats a symbol as part of the API only if client code can access it without reflection or other tricks.</p> <ul> <li>Visibility: <code>public</code> and <code>protected</code> declarations are candidates; <code>private</code> and package-private declarations are not. A nested type or member must itself be accessible, and its enclosing type must be accessible as well.</li> <li>Module boundaries: In a named module, a package must appear in an unqualified <code>exports</code> directive to contribute to the API surface. Qualified exports (<code>exports ... to ...</code>) are not considered public API.</li> <li>No module declaration: If the library has no <code>module-info</code>, Roseau treats all packages as accessible.</li> </ul> <p>Package-private declarations</p> <p>Package-private declarations are not considered API, even if a client could technically access them by placing code in the same package or using other non-standard means.</p>"},{"location":"api-surface/#types","title":"Types","text":"<p>Public top-level and nested types are part of the API surface when their package is exported (or the library is not modular).</p> <p>Protected nested types require an additional check. A protected nested type is only accessible to subclasses of its enclosing type, so Roseau treats a protected nested type as API only when its enclosing type is not effectively final.</p> <p>A type is effectively final when client code cannot subclass it:</p> <ul> <li>the type is declared <code>final</code></li> <li>the type is part of a <code>sealed</code> hierarchy (but is not <code>non-sealed</code>)</li> <li>the type is an <code>enum</code> or a <code>record</code>, both of which are implicitly final</li> <li>the type is a class whose only declared constructors are <code>private</code> (i.e., it has no <code>public</code> or <code>protected</code> subclass-accessible constructor)</li> </ul> <p>These conditions are evaluated at every level of nesting. For a deeply nested type to be part of the API, each type in its enclosing chain must independently satisfy the applicable accessibility conditions.</p>"},{"location":"api-surface/#members","title":"Members","text":"<p>Public fields, methods, and constructors are part of the API when they belong to an API type. Protected members are part of the API only when both conditions hold:</p> <ul> <li>their containing type is itself part of the API surface</li> <li>their containing type is not effectively final (a type that cannot be subclassed makes its protected members unreachable to clients)</li> </ul>"},{"location":"api-surface/#user-configured-exclusions","title":"User-Configured Exclusions","text":"<p>After applying the baseline visibility and module rules, Roseau applies any exclusions configured by the user. This removes declarations that are technically accessible but should not be treated as supported API, such as:</p> <ul> <li>symbols matching a name pattern like <code>*.internal.*</code></li> <li>symbols carrying a stability annotation like <code>@Beta</code> or <code>@API(status = INTERNAL)</code></li> </ul> <p>Use the Configuration File guide to configure name-based and annotation-based exclusions.</p>"},{"location":"api-surface/#example","title":"Example","text":"<p>Assume Roseau is configured to exclude symbols under <code>com.example.internal.*</code> and symbols annotated <code>@Beta</code> or <code>@API(status = INTERNAL)</code>, and the library declares the following module:</p> module-info.java<pre><code>module com.example.lib {\n    exports com.example.api;      // API package\n    exports com.example.internal; // Still not API: excluded by name\n    // com.example.impl is not exported, so it is not API\n}\n</code></pre> <p>Roseau classifies the following declarations as API or not:</p> <pre><code>package com.example.api;\n\nimport com.google.common.annotations.Beta;\nimport org.apiguardian.api.API;\n\npublic class C1 { // API\n    private String f1; // Not API: private\n    public void m1() {} // API\n    protected void m2() {} // API: protected member of a non-effectively-final type\n    protected static class N {} // API: protected nested type within a non-effectively-final type\n}\n\nclass C2 {} // Not API: package-private\n\npublic final class C3 { // API\n    protected String f1; // Not API: protected member of an effectively-final type\n    public void m1() {} // API\n    protected void m2() {} // Not API: protected member of an effectively-final type\n}\n\n@Beta\npublic class C4 {} // Not API: excluded by annotation\n\n@API(status = API.Status.INTERNAL)\npublic class C5 {} // Not API: excluded by annotation\n</code></pre> <pre><code>package com.example.internal;\n\npublic class C { // Not API: excluded by name\n    public void m() {} // Not API\n}\n</code></pre> <pre><code>package com.example.impl;\n\npublic class C { // Not API: package not exported by the module\n    public void hiddenFromClients() {} // Not API\n}\n</code></pre>"},{"location":"breaking-change-kinds/","title":"Kinds of breaking changes","text":"<p>This catalog lists every breaking change kind Roseau reports.</p> <p>Detection rules</p> <p>Roseau's detection rules aim to be as accurate as possible, avoiding false positives and false negatives. Many edge cases involving complex interactions are not covered on this page. While it describes the most common cases, the full detection rules can be reviewed in the source code, with examples in the corresponding test cases.</p>"},{"location":"breaking-change-kinds/#conventions","title":"Conventions","text":"<p>Breaking changes are expressed at the API level, not the code level. Roseau works with the API surface it extracted, not the raw source code or bytecode is analyzes. For example, a <code>public</code> type that becomes <code>package-private</code> is treated the same as a type that is removed from the codebase: it is reported as <code>TYPE_REMOVED</code>. From Roseau's perspective the type isn't API anymore. Likewise, changes to declarations that are not part of the API surface are ignored and not checked for breaking changes. This includes, for instance, <code>protected</code> members of effectively-final types.</p> <p>In the following, a breaking change on an executable applies to both methods and constructors.</p> <p>Each entry shows the smallest library change that triggers the kind, and a minimal example of client code affected by it. One library edit can trigger multiple kinds, so a real diff may report several related findings for the same change.</p> <p>Compatibility icons used throughout this page:</p> <p> Binary \u2014 breaks already compiled clients (<code>.class</code> files linked against the old API)</p> <p> Source \u2014 breaks clients on recompilation (source code that compiled against the old API)</p> <p>An absent icon means the change does not affect compatibility in that category.</p> <p>Jump to: Type-Related \u00b7 Class-Related \u00b7 Annotation-Related \u00b7 Field-Related \u00b7 Executable-Related \u00b7 Method-Related \u00b7 Formal Type Parameters</p>"},{"location":"breaking-change-kinds/#summary","title":"Summary","text":"Kind Trigger <code>TYPE_REMOVED</code> A visible type is removed from the API <code>TYPE_NOW_PROTECTED</code> A public nested type becomes protected <code>TYPE_KIND_CHANGED</code> A type changes its kind (e.g., changing a class to an enum or a record) <code>TYPE_SUPERTYPE_REMOVED</code> A visible supertype is removed from a type's hierarchy <code>TYPE_NEW_ABSTRACT_METHOD</code> An abstract class or interface gains a new abstract method <code>CLASS_NOW_ABSTRACT</code> A concrete class becomes abstract <code>CLASS_NOW_FINAL</code> A class becomes effectively final <code>CLASS_NOW_CHECKED_EXCEPTION</code> An unchecked exception becomes checked <code>CLASS_NOW_STATIC</code> A nested inner class becomes static <code>CLASS_NO_LONGER_STATIC</code> A nested static class becomes inner <code>ANNOTATION_TARGET_REMOVED</code> An annotation loses one or more legal targets <code>ANNOTATION_NEW_METHOD_WITHOUT_DEFAULT</code> An annotation gains an element with no default value <code>ANNOTATION_NO_LONGER_REPEATABLE</code> An annotation stops being repeatable <code>ANNOTATION_METHOD_NO_LONGER_DEFAULT</code> An annotation element loses its default value <code>FIELD_REMOVED</code> A visible field is removed from the API <code>FIELD_NOW_PROTECTED</code> A public field becomes protected <code>FIELD_TYPE_ERASURE_CHANGED</code> A field type change alters the erased field descriptor <code>FIELD_TYPE_CHANGED_INCOMPATIBLE</code> A field type change breaks reads or writes <code>FIELD_NOW_FINAL</code> A writable field becomes final <code>FIELD_NOW_STATIC</code> An instance field becomes static <code>FIELD_NO_LONGER_STATIC</code> A static field becomes an instance field <code>EXECUTABLE_REMOVED</code> An executable is removed from the API <code>EXECUTABLE_NOW_PROTECTED</code> A public executable becomes protected <code>EXECUTABLE_NOW_THROWS_CHECKED_EXCEPTION</code> An executable adds or widens a checked exception <code>EXECUTABLE_NO_LONGER_THROWS_CHECKED_EXCEPTION</code> An executable removes or narrows a checked exception <code>EXECUTABLE_PARAMETER_GENERICS_CHANGED</code> Parameter generic arguments change while erasure stays the same <code>METHOD_RETURN_TYPE_ERASURE_CHANGED</code> A return type change alters the erased method signature <code>METHOD_RETURN_TYPE_CHANGED_INCOMPATIBLE</code> A return type change breaks callers or overriders <code>METHOD_NOW_ABSTRACT</code> A concrete method becomes abstract <code>METHOD_NOW_FINAL</code> An overridable method becomes final <code>METHOD_NOW_STATIC</code> An instance method becomes static <code>METHOD_OVERRIDABLE_NOW_STATIC</code> An overridable or interface method becomes static <code>METHOD_NO_LONGER_STATIC</code> A static method becomes an instance method <code>FORMAL_TYPE_PARAMETER_ADDED</code> A formal type parameter is added to a declaration <code>FORMAL_TYPE_PARAMETER_REMOVED</code> A formal type parameter is removed from a declaration <code>FORMAL_TYPE_PARAMETER_CHANGED</code> A declaration's formal type parameter changes bounds in an incompatible way"},{"location":"breaking-change-kinds/#type-related","title":"Type-Related","text":""},{"location":"breaking-change-kinds/#type_removed","title":"<code>TYPE_REMOVED</code>","text":"<p>An exported type disappears from the API. This includes a <code>public</code> top-level type becoming <code>package-private</code> or a nested type no longer visible.</p> <p>Library</p> <pre><code>-public class A {}\n</code></pre> <p>Client</p> <pre><code>new A();\n</code></pre>"},{"location":"breaking-change-kinds/#type_now_protected","title":"<code>TYPE_NOW_PROTECTED</code>","text":"<p>A public nested type becomes protected. Code outside the allowed inheritance context can no longer reference it.</p> <p>Library</p> <pre><code> public class A {\n-  public class B {}\n+  protected class B {}\n }\n</code></pre> <p>Client</p> <pre><code>A.B b;\n</code></pre>"},{"location":"breaking-change-kinds/#type_kind_changed","title":"<code>TYPE_KIND_CHANGED</code>","text":"<p>A type changes kind \u2014 for example from a class to an interface, an enum, or a record. The rules governing how the type can be instantiated, extended, or implemented change fundamentally, breaking clients that relied on the original kind.</p> <p>Library</p> <pre><code>-public class A {}\n+public interface A {}\n</code></pre> <p>Client</p> <pre><code>A a = new A();\n</code></pre>"},{"location":"breaking-change-kinds/#type_supertype_removed","title":"<code>TYPE_SUPERTYPE_REMOVED</code>","text":"<p>An exported supertype is removed from a type's hierarchy. Roseau reports the closest exported supertype that no longer appears in the new hierarchy. Upcasts and API contracts based on the removed supertype stop working.</p> <p>Library</p> <pre><code> public class A {}\n-public class B extends A {}\n+public class B {}\n</code></pre> <p>Client</p> <pre><code>A a = new B();\n</code></pre>"},{"location":"breaking-change-kinds/#type_new_abstract_method","title":"<code>TYPE_NEW_ABSTRACT_METHOD</code>","text":"<p>An abstract class or interface gains a new abstract method. Existing source-level implementations must provide it.</p> <p>Library</p> <pre><code>-public interface I {}\n+public interface I {\n+  void m();\n+}\n</code></pre> <p>Client</p> <pre><code>I i = new I() {};\n</code></pre> <p>Why source-only</p> <p>Compiled classes that already implement the interface continue to link and load \u2014 the JVM does not verify method completeness when loading a class. Only recompilation of implementing classes forces the new method to be added.</p>"},{"location":"breaking-change-kinds/#class-related","title":"Class-Related","text":""},{"location":"breaking-change-kinds/#class_now_abstract","title":"<code>CLASS_NOW_ABSTRACT</code>","text":"<p>A concrete class becomes abstract. Clients that instantiate it directly can no longer compile or link.</p> <p>Library</p> <pre><code>-public class A {}\n+public abstract class A {}\n</code></pre> <p>Client</p> <pre><code>new A();\n</code></pre>"},{"location":"breaking-change-kinds/#class_now_final","title":"<code>CLASS_NOW_FINAL</code>","text":"<p>A class that was not effectively final becomes effectively final, preventing subclassing. This fires for explicit <code>final</code>, for <code>sealed</code> (without <code>non-sealed</code>), and for any change that removes all constructors accessible to subclasses.</p> <p>Library</p> <pre><code>-public class A {}\n+public final class A {}\n</code></pre> <p>Client</p> <pre><code>class B extends A {}\n</code></pre>"},{"location":"breaking-change-kinds/#class_now_checked_exception","title":"<code>CLASS_NOW_CHECKED_EXCEPTION</code>","text":"<p>An exception class that extended <code>RuntimeException</code> (directly or indirectly) now extends <code>Exception</code> (directly or indirectly). Callers must catch or declare it.</p> <p>Library</p> <pre><code>-public class A extends RuntimeException {}\n+public class A extends Exception {}\n</code></pre> <p>Client</p> <pre><code>throw new A();\n</code></pre>"},{"location":"breaking-change-kinds/#class_now_static","title":"<code>CLASS_NOW_STATIC</code>","text":"<p>A nested inner class becomes static. Source code using the qualified instantiation form <code>outer.new B()</code> no longer compiles. Compiled code also breaks: an inner class constructor carries a hidden enclosing-instance parameter that disappears when the class becomes static, so compiled call sites fail to link.</p> <p>Library</p> <pre><code> public class A {\n-  public class B {}\n+  public static class B {}\n }\n</code></pre> <p>Client</p> <pre><code>new A().new B();\n</code></pre>"},{"location":"breaking-change-kinds/#class_no_longer_static","title":"<code>CLASS_NO_LONGER_STATIC</code>","text":"<p>A nested static class becomes inner. Clients now need an enclosing instance to create it, breaking both the source form and compiled code.</p> <p>Library</p> <pre><code> public class A {\n-  public static class B {}\n+  public class B {}\n }\n</code></pre> <p>Client</p> <pre><code>new A.B();\n</code></pre>"},{"location":"breaking-change-kinds/#annotation-related","title":"Annotation-Related","text":""},{"location":"breaking-change-kinds/#annotation_target_removed","title":"<code>ANNOTATION_TARGET_REMOVED</code>","text":"<p>An annotation loses one or more valid placement targets. Existing uses at those targets no longer compile.</p> <p>Library</p> <pre><code> @java.lang.annotation.Target({\n-  java.lang.annotation.ElementType.FIELD,\n-  java.lang.annotation.ElementType.LOCAL_VARIABLE\n+  java.lang.annotation.ElementType.FIELD\n })\n public @interface A {}\n</code></pre> <p>Client</p> <pre><code>@A int i;\n</code></pre>"},{"location":"breaking-change-kinds/#annotation_new_method_without_default","title":"<code>ANNOTATION_NEW_METHOD_WITHOUT_DEFAULT</code>","text":"<p>An annotation gains a new element with no default value. Existing annotation uses must now supply a value for the new element.</p> <p>Library</p> <pre><code> public @interface A {\n   int i();\n+  String s();\n }\n</code></pre> <p>Client</p> <pre><code>@A(i = 0) int a;\n</code></pre>"},{"location":"breaking-change-kinds/#annotation_no_longer_repeatable","title":"<code>ANNOTATION_NO_LONGER_REPEATABLE</code>","text":"<p>An annotation loses <code>@Repeatable</code>. Sites that use the annotation more than once no longer compile.</p> <p>Library</p> <pre><code>-@java.lang.annotation.Repeatable(Container.class)\n public @interface A {}\n</code></pre> <p>Client</p> <pre><code>@A @A int a;\n</code></pre>"},{"location":"breaking-change-kinds/#annotation_method_no_longer_default","title":"<code>ANNOTATION_METHOD_NO_LONGER_DEFAULT</code>","text":"<p>An existing annotation element loses its default value. Annotation uses that relied on the default must now supply a value explicitly.</p> <p>Library</p> <pre><code> public @interface A {\n-  String value() default \"\";\n+  String value();\n }\n</code></pre> <p>Client</p> <pre><code>@A int a;\n</code></pre>"},{"location":"breaking-change-kinds/#field-related","title":"Field-Related","text":""},{"location":"breaking-change-kinds/#field_removed","title":"<code>FIELD_REMOVED</code>","text":"<p>A visible field disappears from the API and can no longer be accessed in client code.</p> <p>Library</p> <pre><code> public class A {\n-  public int f;\n }\n</code></pre> <p>Client</p> <pre><code>int x = new A().f;\n</code></pre>"},{"location":"breaking-change-kinds/#field_now_protected","title":"<code>FIELD_NOW_PROTECTED</code>","text":"<p>A public field becomes protected. Code outside subclasses or the package loses access.</p> <p>Library</p> <pre><code> public class A {\n-  public int f;\n+  protected int f;\n }\n</code></pre> <p>Client</p> <pre><code>int x = new A().f;\n</code></pre>"},{"location":"breaking-change-kinds/#field_type_erasure_changed","title":"<code>FIELD_TYPE_ERASURE_CHANGED</code>","text":"<p>A field's type changes in a way that alters its erased descriptor. Recompilation can still succeed (for example via autoboxing), but compiled bytecode that references the old field descriptor fails at link time. Fields that are compile-time constants (<code>public static final</code> fields of a primitive type or <code>String</code> initialized to a constant expression) are excluded: the Java compiler inlines their value at every use site and emits no field-access instruction, so the field descriptor is irrelevant to compiled clients.</p> <p>Library</p> <pre><code> public class A {\n-  public int f;\n+  public Integer f;\n }\n</code></pre> <p>Client</p> <pre><code>int x = new A().f;\n</code></pre> <p>Why binary-only</p> <p>Java source allows reading an <code>Integer</code> field into an <code>int</code> variable via unboxing, so recompilation succeeds. Compiled bytecode carries the old field descriptor (<code>I</code> vs <code>Ljava/lang/Integer;</code>) and fails when the JVM resolves the field.</p>"},{"location":"breaking-change-kinds/#field_type_changed_incompatible","title":"<code>FIELD_TYPE_CHANGED_INCOMPATIBLE</code>","text":"<p>A field's type changes in a way that breaks source-level reads or writes, even though the erased descriptor is unchanged.</p> <p>Library</p> <pre><code> public class A {\n-  public java.util.List&lt;String&gt; f;\n+  public java.util.List&lt;Integer&gt; f;\n }\n</code></pre> <p>Client</p> <pre><code>java.util.List&lt;String&gt; xs = new A().f;\n</code></pre>"},{"location":"breaking-change-kinds/#field_now_final","title":"<code>FIELD_NOW_FINAL</code>","text":"<p>A writable field becomes final. Client code that assigns to the field can no longer compile or link.</p> <p>Library</p> <pre><code> public class A {\n-  public int f;\n+  public final int f = 0;\n }\n</code></pre> <p>Client</p> <pre><code>new A().f = 1;\n</code></pre>"},{"location":"breaking-change-kinds/#field_now_static","title":"<code>FIELD_NOW_STATIC</code>","text":"<p>An instance field becomes static. Compiled <code>getfield</code> instructions are incompatible with a static field and fail at link time.</p> <p>Library</p> <pre><code> public class A {\n-  public int f;\n+  public static int f;\n }\n</code></pre> <p>Client</p> <pre><code>A a = new A();\nint x = a.f;\n</code></pre> <p>Why binary-only</p> <p>Java source allows accessing a static field through an instance reference (with a warning), so recompilation usually succeeds. Compiled bytecode uses <code>getfield</code>, which is incompatible with a static field.</p>"},{"location":"breaking-change-kinds/#field_no_longer_static","title":"<code>FIELD_NO_LONGER_STATIC</code>","text":"<p>A static field becomes an instance field. Static access no longer compiles, and compiled <code>getstatic</code> instructions fail at link time.</p> <p>Library</p> <pre><code> public class A {\n-  public static int f;\n+  public int f;\n }\n</code></pre> <p>Client</p> <pre><code>int x = A.f;\n</code></pre>"},{"location":"breaking-change-kinds/#executable-related","title":"Executable-Related","text":""},{"location":"breaking-change-kinds/#executable_removed","title":"<code>EXECUTABLE_REMOVED</code>","text":"<p>A visible method or constructor disappears from the API.</p> <p>Library</p> <pre><code> public class A {\n-  public void m() {}\n }\n</code></pre> <p>Client</p> <pre><code>new A().m();\n</code></pre>"},{"location":"breaking-change-kinds/#executable_now_protected","title":"<code>EXECUTABLE_NOW_PROTECTED</code>","text":"<p>A public method or constructor becomes protected. Callers outside the allowed inheritance context lose access.</p> <p>Library</p> <pre><code> public class A {\n-  public void m() {}\n+  protected void m() {}\n }\n</code></pre> <p>Client</p> <pre><code>new A().m();\n</code></pre>"},{"location":"breaking-change-kinds/#executable_now_throws_checked_exception","title":"<code>EXECUTABLE_NOW_THROWS_CHECKED_EXCEPTION</code>","text":"<p>A method or constructor adds a checked exception that is not already covered by an existing entry in the throws clause. Callers must catch or declare it.</p> <p>Library</p> <pre><code> public class A {\n-  public void m() {}\n+  public void m() throws java.io.IOException {}\n }\n</code></pre> <p>Client</p> <pre><code>new A().m();\n</code></pre>"},{"location":"breaking-change-kinds/#executable_no_longer_throws_checked_exception","title":"<code>EXECUTABLE_NO_LONGER_THROWS_CHECKED_EXCEPTION</code>","text":"<p>A checked exception is removed or narrowed in a way that breaks overriders or callers.</p> <ul> <li>For non-final methods: every old checked exception must still be covered by the same or a broader exception in the new signature. Narrowing to a subtype (e.g., <code>IOException \u2192 FileNotFoundException</code>) counts as a breaking removal because subclasses that declared the old exception can no longer do so.</li> <li>For final methods or constructors: narrowing to a subtype is allowed; only complete removal breaks callers.</li> </ul> <p>Library</p> <pre><code> public class A {\n-  public void m() throws java.io.IOException {}\n+  public void m() {}\n }\n</code></pre> <p>Client</p> <pre><code>try {\n  new A().m();\n} catch (java.io.IOException e) {}\n</code></pre> <p>Removing an exception is a source break</p> <p>Removing a checked exception from a non-final method is a source break for two reasons. First, subclasses that override the method may declare the same exception in their <code>throws</code> clause \u2014 once the parent removes it, those overrides no longer compile. Second, callers that catch the exception are also broken: Java makes it a compile error (not just a warning) to have a <code>catch</code> block for a checked exception that the callee can no longer throw.</p>"},{"location":"breaking-change-kinds/#executable_parameter_generics_changed","title":"<code>EXECUTABLE_PARAMETER_GENERICS_CHANGED</code>","text":"<p>Parameter generic arguments change while the erased signature stays the same.</p> <ul> <li>For overridable methods, any change to generic arguments is a source break: subclasses overriding with the old signature would have mismatched parameter types.</li> <li>For effectively final methods or constructors, Roseau applies a variance check: the new parameter type must be a supertype of the old to be compatible.</li> </ul> <p>Library</p> <pre><code> public class A {\n-  public final void m(java.util.List&lt;String&gt; xs) {}\n+  public final void m(java.util.List&lt;Integer&gt; xs) {}\n }\n</code></pre> <p>Client</p> <pre><code>new A().m(java.util.List.of(\"x\"));\n</code></pre>"},{"location":"breaking-change-kinds/#method-related","title":"Method-Related","text":""},{"location":"breaking-change-kinds/#method_return_type_erasure_changed","title":"<code>METHOD_RETURN_TYPE_ERASURE_CHANGED</code>","text":"<p>A method's return type changes in a way that alters its erased signature. Recompilation can still succeed (for example via autoboxing), but compiled bytecode linked against the old method descriptor fails at link time.</p> <p>Library</p> <pre><code> public class A {\n-  public final int m() { return 0; }\n+  public final Integer m() { return 0; }\n }\n</code></pre> <p>Client</p> <pre><code>int x = new A().m();\n</code></pre>"},{"location":"breaking-change-kinds/#method_return_type_changed_incompatible","title":"<code>METHOD_RETURN_TYPE_CHANGED_INCOMPATIBLE</code>","text":"<p>A method's return type changes in a way that breaks callers or overriders at the source level, even when erasure is unchanged.</p> <p>Callers break when the new return type cannot be used in place of the old one \u2014 for instance, when a caller assigns the return value to a variable typed with the old return type. Overriders break when the new return type is not a supertype of the old return type. Java's covariant override rule requires the overriding method's return type to be a subtype of the overridden method's; if the parent's return type narrows, subclasses that still declare the old (broader) type violate this rule.</p> <p>Library</p> <pre><code> public class A&lt;T, U&gt; {\n-  public T m() { return null; }\n+  public U m() { return null; }\n }\n</code></pre> <p>Client</p> <pre><code>Integer x = new A&lt;Integer, String&gt;().m();\n</code></pre>"},{"location":"breaking-change-kinds/#method_now_abstract","title":"<code>METHOD_NOW_ABSTRACT</code>","text":"<p>A concrete method becomes abstract. Subclasses or anonymous classes that do not implement it can no longer be compiled or instantiated.</p> <p>Library</p> <pre><code> public abstract class A {\n-  public void m() {}\n+  public abstract void m();\n }\n</code></pre> <p>Client</p> <pre><code>new A() {};\n</code></pre>"},{"location":"breaking-change-kinds/#method_now_final","title":"<code>METHOD_NOW_FINAL</code>","text":"<p>An overridable method becomes final. Existing overrides in subclasses no longer compile, and compiled override call sites fail at link time.</p> <p>Library</p> <pre><code> public class A {\n-  public void m() {}\n+  public final void m() {}\n }\n</code></pre> <p>Client</p> <pre><code>class B extends A {\n  @Override public void m() {}\n}\n</code></pre>"},{"location":"breaking-change-kinds/#method_now_static","title":"<code>METHOD_NOW_STATIC</code>","text":"<p>An instance method becomes static. Compiled <code>invokevirtual</code> instructions are incompatible with a static method and fail at link time.</p> <p>Library</p> <pre><code> public final class A {\n-  public void m() {}\n+  public static void m() {}\n }\n</code></pre> <p>Client</p> <pre><code>new A().m();\n</code></pre> <p>Why binary-only</p> <p>Java source allows calling a static method through an instance reference (with a warning), so recompilation usually succeeds. Compiled bytecode uses <code>invokevirtual</code>, which is incompatible with a <code>static</code> method. See <code>METHOD_OVERRIDABLE_NOW_STATIC</code> for the additional source-breaking case when the method was overridable.</p>"},{"location":"breaking-change-kinds/#method_overridable_now_static","title":"<code>METHOD_OVERRIDABLE_NOW_STATIC</code>","text":"<p>An overridable method \u2014 or any interface method \u2014 becomes static. Subclasses or implementors that override it can no longer do so. Roseau reports this alongside <code>METHOD_NOW_STATIC</code> when the old method could be overridden.</p> <p>Library</p> <pre><code> public class A {\n-  public void m() {}\n+  public static void m() {}\n }\n</code></pre> <p>Client</p> <pre><code>class B extends A {\n  @Override public void m() {}\n}\n</code></pre>"},{"location":"breaking-change-kinds/#method_no_longer_static","title":"<code>METHOD_NO_LONGER_STATIC</code>","text":"<p>A static method becomes an instance method. Static calls no longer compile, and compiled <code>invokestatic</code> instructions fail at link time.</p> <p>Library</p> <pre><code> public class A {\n-  public static void m() {}\n+  public void m() {}\n }\n</code></pre> <p>Client</p> <pre><code>A.m();\n</code></pre>"},{"location":"breaking-change-kinds/#formal-type-parameters","title":"Formal Type Parameters","text":""},{"location":"breaking-change-kinds/#formal_type_parameter_added","title":"<code>FORMAL_TYPE_PARAMETER_ADDED</code>","text":"<p>A type, method, or constructor gains an extra formal type parameter, changing its generic arity. Existing source-level uses with the old arity no longer compile.</p> <p>Library</p> <pre><code>-public class A&lt;T&gt; {}\n+public class A&lt;T, U&gt; {}\n</code></pre> <p>Client</p> <pre><code>A&lt;String&gt; a;\n</code></pre>"},{"location":"breaking-change-kinds/#formal_type_parameter_removed","title":"<code>FORMAL_TYPE_PARAMETER_REMOVED</code>","text":"<p>A type, method, or constructor loses a formal type parameter, changing its generic arity. Existing source-level uses with the old arity no longer compile.</p> <p>Library</p> <pre><code>-public class A&lt;T, U&gt; {}\n+public class A&lt;T&gt; {}\n</code></pre> <p>Client</p> <pre><code>A&lt;String, Integer&gt; a;\n</code></pre>"},{"location":"breaking-change-kinds/#formal_type_parameter_changed","title":"<code>FORMAL_TYPE_PARAMETER_CHANGED</code>","text":"<p>Generic bounds change incompatibly. The check depends on what is being changed:</p> <ul> <li>For types and non-overridable executables (constructors, or methods on effectively-final types): each new bound must be a supertype of some old bound. Tightening a bound \u2014 replacing it with a more specific type \u2014 narrows the set of valid type arguments and breaks existing uses.</li> <li>For overridable methods: bounds must be exactly equal after accounting for any type parameter renames. Any change to bounds is a break, because a subclass overriding the method with the old bounds would no longer match the new signature.</li> </ul> <p>Previously valid type arguments may no longer satisfy the new bounds.</p> <p>Library</p> <pre><code>-public class A&lt;T extends CharSequence&gt; {}\n+public class A&lt;T extends String&gt; {}\n</code></pre> <p>Client</p> <pre><code>A&lt;StringBuilder&gt; a = new A&lt;&gt;();\n</code></pre>"},{"location":"quickstart/","title":"Quickstart","text":""},{"location":"quickstart/#1-get-roseau","title":"1. Get Roseau","text":"Standalone (no JDK required)JAR (requires Java 25)Build from source <p>Download the archive for your platform from the latest release, unzip it, and add the <code>bin/</code> directory to your <code>PATH</code>.</p> <pre><code>unzip roseau-&lt;version&gt;-linux-x86_64.zip\nexport PATH=\"$PWD/roseau-&lt;version&gt;/bin:$PATH\"\n</code></pre> <p>Verify the installation:</p> <pre><code>roseau --version\n</code></pre> <p>Download <code>roseau-&lt;version&gt;.jar</code> from the latest release.</p> <pre><code>java -jar roseau-&lt;version&gt;.jar --version\n</code></pre> <p>The examples on this page use the <code>roseau</code> command. Replace it with <code>java -jar roseau-&lt;version&gt;.jar</code> when using the JAR directly.</p> <p>Roseau can be cloned and built from source.</p> <pre><code>git clone https://github.com/alien-tools/roseau.git\ncd roseau\n./mvnw --batch-mode package\nalias roseau='java -jar $PWD/cli/target/roseau-&lt;version&gt;.jar'\n</code></pre>"},{"location":"quickstart/#2-run-a-first-diff","title":"2. Run a First Diff","text":"<pre><code>roseau --diff --v1 path/to/v1.jar --v2 path/to/v2.jar\n</code></pre> <p><code>--v1</code> and <code>--v2</code> can each be a JAR file or a source directory (typically similar to <code>src/main/java</code>).</p> <p>Roseau compares the library API surface, not every declaration in the input. For the exact rules, see what Roseau considers API.</p>"},{"location":"quickstart/#3-read-the-result","title":"3. Read the Result","text":"<p>Typical CLI output looks like this:</p> <pre><code>Breaking changes found: 3 (2 binary-breaking, 2 source-breaking)\n\u2717 com.pkg.A TYPE_REMOVED\n  \u2717 binary-breaking \u2717 source-breaking\n  \u2192 com/pkg/A.java:4\n\u26a0 com.pkg.B.f FIELD_NOW_STATIC\n  \u2717 binary-breaking \u2713 source-compatible\n  \u2192 com/pkg/B.java:18\n\u2605 com.pkg.C TYPE_NEW_ABSTRACT_METHOD [toOverride()]\n  \u2713 binary-compatible \u2717 source-breaking\n  \u2192 com/pkg/C.java:210\n</code></pre> <p>Each entry begins with a symbol denoting the nature of the change (\u2717 removal, \u26a0 change, \u2605 addition), followed by the affected symbol (e.g., a type, a method, a field) and the kind of breaking change that was identified. The second line shows binary and source compatibility. The third line gives the location of the impacted symbol in the first version.</p>"},{"location":"quickstart/#4-write-report-files","title":"4. Write Report Files","text":"<p>Roseau can generate reports in various formats.</p> <pre><code>roseau --diff --v1 path/to/v1.jar --v2 path/to/v2.jar \\\n  --report CSV=report.csv --report HTML=report.html\n</code></pre> <p><code>--report=FORMAT=PATH</code> can be repeated. Available formats: <code>CLI</code>, <code>CSV</code>, <code>HTML</code>, <code>JSON</code>, <code>MD</code>.</p>"},{"location":"quickstart/#5-use-roseau-in-ci-or-to-gate-a-build","title":"5. Use Roseau in CI or to gate a build","text":"<p>By default, Roseau does not exit with an error code when breaking changes are identified. The <code>--fail-on-bc</code> flag can be used for that purpose. Together with <code>--plain</code>, it can be used to gate a build in CI.</p> <pre><code>roseau --diff --v1 path/to/v1.jar --v2 path/to/v2.jar --fail-on-bc --plain\n</code></pre> <p><code>--fail-on-bc</code> exits with code <code>1</code> when breaking changes are found. <code>--plain</code> strips ANSI colors for log files and CI output.</p>"},{"location":"quickstart/#6-specify-api-exclusions","title":"6. Specify API exclusions","text":"<p>Use <code>excludes</code> when some symbols are technically accessible by client code but should not be treated as supported API. Roseau does not report any breaking change affecting excluded symbols. If a type is excluded, its nested declarations and members are excluded as well.</p> <p>Common examples include package or symbol naming conventions such as <code>com\\.example\\.internal\\..*</code> and stability annotations such as <code>com.google.common.annotations.Beta</code> or <code>org.apiguardian.api.API(status = INTERNAL)</code>. These can be configured in the YAML configuration file:</p> <pre><code>common:\n  excludes:\n    names:\n      - com\\.example\\.internal\\..*\n    annotations:\n      - name: com.google.common.annotations.Beta\n      - name: org.apiguardian.api.API\n        args: { status: org.apiguardian.api.API$Status.INTERNAL }\n</code></pre>"},{"location":"quickstart/#next-steps","title":"Next Steps","text":"Question Page Which input pair fits the workflow? Compare Two Versions Which declarations are compared? API Surface Which report format to use? Report Formats What breaking change kinds can appear? Breaking Change Kinds"},{"location":"guides/ci/","title":"Use in CI","text":"<p>Roseau is meant to be easily integrated into build and CI pipelines to spot breaking changes as early as possible. A typical example is as follows:</p> <pre><code>roseau --diff --v1 path/to/v1.jar --v2 path/to/v2.jar --fail-on-bc --plain \\\n  --report JSON=reports/breaking-changes.json\n</code></pre> <p><code>--fail-on-bc</code> makes the process fail when breaking changes are found. <code>--plain</code> removes ANSI control codes from the log output. The <code>--report</code> option generates a machine-readable JSON report.</p>"},{"location":"guides/ci/#exit-codes","title":"Exit Codes","text":"Code Meaning <code>0</code> the check completed and no breaking changes were found <code>1</code> <code>--fail-on-bc</code> was set and breaking changes were found <code>2</code> the command failed before producing a result"},{"location":"guides/compare/","title":"Comparing two versions","text":"<p>Roseau compares the API surface of two library versions. The inputs can be JAR files, source trees, or one of each. For the exact API-surface rules, see API Surface, and YAML configuration file for configuring library-specific API exclusions. Roseau's output can be filtered to show only binary-breaking (<code>--binary-only</code>) or source-breaking (<code>--source-only</code>) changes.</p>"},{"location":"guides/compare/#base-command","title":"Base Command","text":"<pre><code>roseau --diff --v1 &lt;old-version&gt; --v2 &lt;new-version&gt;\n</code></pre> <p>The old and new versions can be JAR files, source trees (e.g., <code>src/main/java</code>), or Maven coordinates (<code>groupId:artifactId:version</code>):</p> Workflow Example JAR vs JAR <code>--v1 library-1.0.0.jar --v2 library-2.0.0.jar</code> source vs source <code>--v1 project-v1/src --v2 project-v2/src</code> JAR vs source <code>--v1 library-1.0.0.jar --v2 project-v2/src</code> Maven coordinates <code>--v1 com.example:lib:1.0.0 --v2 com.example:lib:2.0.0</code>"},{"location":"guides/compare/#add-dependencies-when-resolution-matters","title":"Add Dependencies When Resolution Matters","text":"<p>Dependency information matters when signatures, supertypes, annotations, or generic types refer to third-party types that are not present in the analyzed inputs.</p> <p>Use a shared classpath when both versions need the same dependencies:</p> <pre><code>roseau --diff --v1 path/to/v1.jar --v2 path/to/v2.jar \\\n  --classpath libs/dependency-a.jar:libs/dependency-b.jar\n</code></pre> <p>Use a shared <code>pom.xml</code> when the classpath should be derived from Maven metadata:</p> <pre><code>roseau --v1 com.example:lib:1.0.0 --v2 com.example:lib:2.0.0 \\\n  --pom path/to/pom.xml\n</code></pre> <p>Use per-version options when the baseline and current version do not share the same dependency set:</p> <pre><code>roseau --diff --v1 path/to/v1/src --v2 path/to/v2/src \\\n  --v1-classpath libs/old-dependency.jar --v2-classpath libs/new-dependency.jar\n</code></pre>"},{"location":"guides/compare/#common-variants","title":"Common Variants","text":"Goal Option plain log output <code>--plain</code> only binary-breaking changes <code>--binary-only</code> only source-breaking changes <code>--source-only</code> accepted-change baseline <code>--ignored accepted.csv</code> report artifacts <code>--report=FORMAT=PATH</code> <p>Note</p> <p><code>--binary-only</code> and <code>--source-only</code> are mutually exclusive.</p>"},{"location":"guides/config/","title":"YAML configuration","text":"<p>When configuration options start piling up, Roseau can read a YAML configuration file with the <code>--config</code> option. The configuration can also refine what Roseau considers part of the API surface, for example by excluding certain symbols based on naming conventions or annotations. For the baseline rules, see What Roseau considers API.</p>"},{"location":"guides/config/#when-a-config-file-helps","title":"When a Config File Helps","text":"<p><code>roseau.yaml</code> is useful when one of these patterns appears repeatedly:</p> <ul> <li>the same classpath or <code>pom.xml</code> is supplied on every run</li> <li>the same exclusions define the supported API surface</li> <li>the same report files are generated in CI or release workflows</li> <li>the same ignored-change baseline is reused against a fixed reference version</li> </ul>"},{"location":"guides/config/#load-the-file","title":"Load the File","text":"<p>The configuration file is loaded using the <code>--config</code> option. Note that in case of conflicts, CLI options take precedence over values configured in the YAML file.</p> <pre><code>roseau --diff --config roseau.yaml --v1 path/to/v1.jar --v2 path/to/v2.jar\n</code></pre>"},{"location":"guides/config/#specifying-api-exclusions","title":"Specifying API exclusions","text":"<p>Use <code>excludes</code> when some symbols are technically accessible by client code but should not be treated as supported API. Roseau does not report any breaking change affecting excluded symbols. If a type is excluded, its nested declarations and members are excluded as well.</p> <p>Common examples:</p> <ul> <li>package or symbol naming conventions such as <code>com\\.example\\.internal\\..*</code></li> <li>stability annotations such as <code>com.google.common.annotations.Beta</code> or <code>org.apiguardian.api.API(status = INTERNAL)</code></li> </ul> roseau.yaml<pre><code>common:\n  excludes:\n    names:\n      - com\\.example\\.internal\\..*\n    annotations:\n      - name: com.google.common.annotations.Beta\n      - name: org.apiguardian.api.API\n        args: { status: org.apiguardian.api.API$Status.INTERNAL }\n</code></pre>"},{"location":"guides/config/#example-configuration","title":"Example configuration","text":"Section Meaning <code>common</code> shared classpath and exclusion rules <code>v1</code> input path and overrides for the baseline version <code>v2</code> input path and overrides for the current version <code>diff</code> filtering options such as <code>ignore</code>, <code>binaryOnly</code>, and <code>sourceOnly</code> <code>reports</code> report files to generate roseau.yaml<pre><code>common:\n  classpath:\n    pom: /path/to/pom.xml\n    jars:\n      - /path/to/dependency.jar\n  excludes:\n    names:\n      - com\\.example\\.internal\\..*\n    annotations:\n      - name: com.google.common.annotations.Beta\n      - name: org.apiguardian.api.API\n        args: { status: org.apiguardian.api.API$Status.INTERNAL }\n\nv1:\n  location: /path/to/library-1.0.0.jar\n\nv2:\n  location: /path/to/library-2.0.0.jar\n\ndiff:\n  ignore: /path/to/ignored.csv\n  binaryOnly: false\n  sourceOnly: false\n\nreports:\n  - file: reports/breaking-changes.csv\n    format: CSV\n  - file: reports/breaking-changes.html\n    format: HTML\n</code></pre>"},{"location":"guides/ignored/","title":"Ignoring known breaking changes","text":"<p>Breaking changes are sometimes unavoidable or intentional. Roseau can therefore be configured to ignore a list of known breaking changes to avoid reporting them in subsequent analyses. The list of ignored changes is specified in a CSV file that shares the same format as the output of the <code>--report</code> option.</p>"},{"location":"guides/ignored/#1-generate-a-baseline-csv","title":"1. Generate a Baseline CSV","text":"<p>Generate a CSV report from the current diff:</p> <p><pre><code>roseau --diff --v1 path/to/v1.jar --v2 path/to/v2.jar \\\n  --report CSV=reports/accepted.csv\n</code></pre> The generated CSV file looks as follows and can be reused to ignore the listed changes in future analyses.</p> <pre><code>type;symbol;kind\npkg.T;pkg.T.m();EXECUTABLE_REMOVED\n</code></pre>"},{"location":"guides/ignored/#2-reuse-the-baseline","title":"2. Reuse the Baseline","text":"<p>Passing that file to the <code>--ignored</code> option will cause Roseau to ignore all changes listed in the CSV file.</p> <pre><code>roseau --diff --v1 path/to/v1.jar --v2 path/to/v2.jar \\\n  --ignored reports/accepted.csv\n</code></pre>"},{"location":"guides/maven-plugin/","title":"Maven plug-in","text":"<p>Roseau's Maven plug-in runs during the <code>verify</code> phase and compares the currently built artifact against a user-defined baseline. It can also be invoked directly after the artifact is built: <code>mvn package roseau:check</code>. This guide covers setup and the most common configurations. The exhaustive parameter list lives in Maven plug-in options.</p>"},{"location":"guides/maven-plugin/#minimal-setup","title":"Minimal Setup","text":"<p>Find the latest released version on Maven Central. Then add the plug-in to <code>pom.xml</code> and provide a baseline:</p> <pre><code>&lt;plugin&gt;\n  &lt;groupId&gt;io.github.alien-tools&lt;/groupId&gt;\n  &lt;artifactId&gt;roseau-maven-plugin&lt;/artifactId&gt;\n  &lt;version&gt;${roseau.version}&lt;/version&gt;\n  &lt;executions&gt;\n    &lt;execution&gt;\n      &lt;goals&gt;\n        &lt;goal&gt;check&lt;/goal&gt;\n      &lt;/goals&gt;\n    &lt;/execution&gt;\n  &lt;/executions&gt;\n  &lt;configuration&gt;\n    &lt;baselineDependency&gt;\n      &lt;groupId&gt;com.example.project&lt;/groupId&gt;\n      &lt;artifactId&gt;example-core&lt;/artifactId&gt;\n      &lt;version&gt;2.1.4&lt;/version&gt;\n    &lt;/baselineDependency&gt;\n  &lt;/configuration&gt;\n&lt;/plugin&gt;\n</code></pre> <p>Then run:</p> <pre><code>mvn verify\n</code></pre> <p>The plug-in runs during the <code>verify</code> phase. It compares the JAR produced by the current build against the baseline and prints any breaking changes to the Maven log.</p> <p>Note</p> <p>The plug-in automatically skips projects with <code>pom</code> packaging.</p>"},{"location":"guides/maven-plugin/#choose-a-baseline","title":"Choose a Baseline","text":"<p>The baseline can be a local JAR file or Maven coordinates resolved from remote repositories.</p> <p>Maven coordinates</p> <p>Use <code>baselineCoordinates</code> to pass coordinates as a single string. This is the recommended form for CI/CD pipelines where the baseline version changes between releases and should not be hard-coded in the POM.</p> <pre><code>&lt;configuration&gt;\n  &lt;baselineCoordinates&gt;com.example.project:example-core:2.1.4&lt;/baselineCoordinates&gt;\n&lt;/configuration&gt;\n</code></pre> <p>It can also be passed on the command line without modifying the POM:</p> <pre><code>mvn verify -Droseau.baselineCoordinates=com.example.project:example-core:2.1.4\n</code></pre> <p>Use <code>baselineDependency</code> for structured XML configuration:</p> <pre><code>&lt;configuration&gt;\n  &lt;baselineDependency&gt;\n    &lt;groupId&gt;com.example.project&lt;/groupId&gt;\n    &lt;artifactId&gt;example-core&lt;/artifactId&gt;\n    &lt;version&gt;2.1.4&lt;/version&gt;\n  &lt;/baselineDependency&gt;\n&lt;/configuration&gt;\n</code></pre> <p>Local JAR file</p> <pre><code>&lt;configuration&gt;\n  &lt;baselineJar&gt;${project.basedir}/old.jar&lt;/baselineJar&gt;\n&lt;/configuration&gt;\n</code></pre> <p>When multiple baseline parameters are set, <code>baselineCoordinates</code> takes precedence over <code>baselineDependency</code>, which takes precedence over <code>baselineJar</code>.</p>"},{"location":"guides/maven-plugin/#multi-module-projects","title":"Multi-module projects","text":"<p>In a multi-module build, the plug-in configuration can be placed once in the parent POM and reused across all modules.</p> <p>The key is that Maven interpolates <code>${project.groupId}</code> and <code>${project.artifactId}</code> at the level of each module, not the parent. Combined with a shared <code>previous.version</code> property, a single configuration covers every module automatically. For instance, using <code>baselineDependency</code>:</p> <pre><code>&lt;!-- Parent POM --&gt;\n&lt;properties&gt;\n  &lt;previous.version&gt;1.2.3&lt;/previous.version&gt;\n&lt;/properties&gt;\n\n&lt;build&gt;\n  &lt;plugins&gt;\n    &lt;plugin&gt;\n      &lt;groupId&gt;io.github.alien-tools&lt;/groupId&gt;\n      &lt;artifactId&gt;roseau-maven-plugin&lt;/artifactId&gt;\n      &lt;version&gt;${roseau.version}&lt;/version&gt;\n      &lt;executions&gt;\n        &lt;execution&gt;\n          &lt;goals&gt;\n            &lt;goal&gt;check&lt;/goal&gt;\n          &lt;/goals&gt;\n        &lt;/execution&gt;\n      &lt;/executions&gt;\n      &lt;configuration&gt;\n        &lt;baselineDependency&gt;\n          &lt;groupId&gt;${project.groupId}&lt;/groupId&gt;\n          &lt;artifactId&gt;${project.artifactId}&lt;/artifactId&gt;\n          &lt;version&gt;${previous.version}&lt;/version&gt;\n        &lt;/baselineDependency&gt;\n      &lt;/configuration&gt;\n    &lt;/plugin&gt;\n  &lt;/plugins&gt;\n&lt;/build&gt;\n</code></pre> <p>The plug-in will automatically resolve the baseline version for each module. New modules that have no previously published version will fail to resolve the baseline. Exclude them individually with <code>roseau.skip</code> or configure Roseau to skip modules with unresolved baselines with <code>roseau.skipIfBaselineUnresolvable</code>.</p>"},{"location":"guides/maven-plugin/#fail-on-incompatible-changes","title":"Fail on incompatible changes","text":"<p>By default, the plug-in reports breaking changes as Maven warnings (<code>[WARN]</code>) but does not fail the build.</p> <pre><code>&lt;configuration&gt;\n  &lt;baselineJar&gt;${project.basedir}/old.jar&lt;/baselineJar&gt;\n  &lt;!-- Fail the build on any breaking change --&gt;\n  &lt;failOnIncompatibility&gt;true&lt;/failOnIncompatibility&gt;\n  &lt;!-- Fail the build on binary-breaking changes --&gt;\n  &lt;failOnBinaryIncompatibility&gt;true&lt;/failOnBinaryIncompatibility&gt;\n  &lt;!-- Fail the build on source-breaking changes --&gt;\n  &lt;failOnSourceIncompatibility&gt;true&lt;/failOnSourceIncompatibility&gt;\n&lt;/configuration&gt;\n</code></pre>"},{"location":"guides/maven-plugin/#narrow-the-scope-of-reported-breaking-changes","title":"Narrow the scope of reported breaking changes","text":"<p>Report only binary-breaking or only source-breaking changes:</p> <pre><code>&lt;configuration&gt;\n  &lt;baselineJar&gt;${project.basedir}/old.jar&lt;/baselineJar&gt;\n  &lt;binaryOnly&gt;true&lt;/binaryOnly&gt;\n&lt;/configuration&gt;\n</code></pre> <p>Or:</p> <pre><code>&lt;configuration&gt;\n  &lt;baselineJar&gt;${project.basedir}/old.jar&lt;/baselineJar&gt;\n  &lt;sourceOnly&gt;true&lt;/sourceOnly&gt;\n&lt;/configuration&gt;\n</code></pre>"},{"location":"guides/maven-plugin/#write-report-artifacts","title":"Write report artifacts","text":"<p>Report files are written under <code>reportDirectory</code>, which defaults to <code>${project.build.directory}/roseau</code>. See Reports for the available formats.</p> <pre><code>&lt;configuration&gt;\n  &lt;baselineJar&gt;${project.basedir}/old.jar&lt;/baselineJar&gt;\n  &lt;reports&gt;\n    &lt;report&gt;\n      &lt;file&gt;report.csv&lt;/file&gt;\n      &lt;format&gt;CSV&lt;/format&gt;\n    &lt;/report&gt;\n    &lt;report&gt;\n      &lt;file&gt;report.html&lt;/file&gt;\n      &lt;format&gt;HTML&lt;/format&gt;\n    &lt;/report&gt;\n  &lt;/reports&gt;\n&lt;/configuration&gt;\n</code></pre>"},{"location":"guides/maven-plugin/#classpath-and-yaml-configuration","title":"Classpath and YAML configuration","text":"<p>The plug-in automatically includes the current project's compile-scope dependencies in the analysis classpath. To add extra entries:</p> <pre><code>&lt;configuration&gt;\n  &lt;baselineJar&gt;${project.basedir}/old.jar&lt;/baselineJar&gt;\n  &lt;classpath&gt;\n    &lt;path&gt;/path/to/common-dependency.jar&lt;/path&gt;\n  &lt;/classpath&gt;\n  &lt;baselineClasspath&gt;\n    &lt;path&gt;/path/to/old-dependency.jar&lt;/path&gt;\n  &lt;/baselineClasspath&gt;\n&lt;/configuration&gt;\n</code></pre> <p>Instead, the Maven plug-in can reuse an existing configuration file that specifies classpaths, exclusion policies, etc. Note that Maven parameters override YAML values.</p> <pre><code>&lt;configuration&gt;\n  &lt;baselineJar&gt;${project.basedir}/old.jar&lt;/baselineJar&gt;\n  &lt;configFile&gt;roseau.yaml&lt;/configFile&gt;\n&lt;/configuration&gt;\n</code></pre>"},{"location":"guides/maven-plugin/#export-api-models","title":"Export API models","text":"<p>Export the API models as JSON for archival or further analysis:</p> <pre><code>&lt;configuration&gt;\n  &lt;baselineJar&gt;${project.basedir}/old.jar&lt;/baselineJar&gt;\n  &lt;exportBaselineApi&gt;${project.build.directory}/roseau/baseline-api.json&lt;/exportBaselineApi&gt;\n  &lt;exportCurrentApi&gt;${project.build.directory}/roseau/current-api.json&lt;/exportCurrentApi&gt;\n&lt;/configuration&gt;\n</code></pre>"},{"location":"guides/reports/","title":"Reports","text":"<p>Roseau can write one or more report files during the same diff run.</p>"},{"location":"guides/reports/#report-syntax","title":"Report Syntax","text":"<p>Reports are generated by passing the <code>--report=FORMAT=PATH</code> option to the CLI one or multiple times. Reports can also be configured in the YAML configuration file.</p> <pre><code>roseau --diff --v1 path/to/v1.jar --v2 path/to/v2.jar \\\n  --report JSON=reports/breaking-changes.json \\\n  --report HTML=reports/breaking-changes.html\n</code></pre> <p>An example HTML report is available here.</p> Format Best fit <code>CLI</code> terminal-style text written to a file <code>CSV</code> review, diffing, and reuse with <code>--ignored</code> <code>HTML</code> human-readable report artifacts <code>JSON</code> automation and post-processing <code>MD</code> Markdown reports for release notes or pull requests"},{"location":"reference/cli/","title":"CLI options","text":"<p>This page documents Roseau's CLI options.</p> <p>Note</p> <p><code>--report=FORMAT=PATH</code> is repeatable. <code>--binary-only</code> and <code>--source-only</code> cannot be combined. Classpath values use the operating-system path separator: <code>:</code> on Unix, <code>;</code> on Windows.</p>"},{"location":"reference/cli/#modes","title":"Modes","text":"<p>Exactly one mode must be specified.</p> Option Meaning <code>--diff</code> Compute breaking changes between versions <code>--v1</code> and <code>--v2</code> <code>--api</code> Extract and serialize the API model of <code>--v1</code> as JSON and prints it to stdout; <code>--api-json=api.json</code> serializes it to a file"},{"location":"reference/cli/#inputs","title":"Inputs","text":"Option Meaning <code>--v1=&lt;path\\|coordinates&gt;</code> First version of the library: a JAR file, a source directory, or Maven coordinates (e.g., <code>com.example:lib:1.0.0</code>) <code>--v2=&lt;path\\|coordinates&gt;</code> Second version of the library: a JAR file, a source directory, or Maven coordinates (<code>--diff</code> mode only) <code>--api-json=&lt;path&gt;</code> Output path for the JSON API model (<code>--api</code> mode only)"},{"location":"reference/cli/#dependencies","title":"Dependencies","text":"Option Meaning <code>--classpath=&lt;path&gt;[,&lt;path&gt;...]</code> Shared classpath for <code>--v1</code> and <code>--v2</code> <code>--pom=&lt;path&gt;</code> Shared <code>pom.xml</code> for <code>--v1</code> and <code>--v2</code> <code>--v1-classpath=&lt;path&gt;[,&lt;path&gt;...]</code> Classpath only for <code>--v1</code> <code>--v2-classpath=&lt;path&gt;[,&lt;path&gt;...]</code> Classpath only for <code>--v2</code> <code>--v1-pom=&lt;path&gt;</code> <code>pom.xml</code> only for <code>--v1</code> <code>--v2-pom=&lt;path&gt;</code> <code>pom.xml</code> only for <code>--v2</code>"},{"location":"reference/cli/#filtering","title":"Filtering","text":"Option Meaning <code>--binary-only</code> Only report binary-breaking changes <code>--source-only</code> Only report source-breaking changes <code>--ignored=&lt;path&gt;</code> Ignore breaking changes listed in a CSV file"},{"location":"reference/cli/#reports","title":"Reports","text":"Option Meaning <code>--report=&lt;format=path&gt;</code> Write a report file in <code>CLI</code>, <code>CSV</code>, <code>HTML</code>, <code>JSON</code>, or <code>MD</code> format; repeatable"},{"location":"reference/cli/#config-and-output","title":"Config and Output","text":"Option Meaning <code>--config=&lt;path&gt;</code> Load options from a <code>roseau.yaml</code> file; CLI options take precedence <code>--fail-on-bc</code> Return exit code <code>1</code> when breaking changes are found <code>--plain</code> Disable ANSI colors, output plain text <code>-v</code>, <code>--verbose</code> Increase verbosity: <code>-v</code> for verbose output, <code>-vv</code> for debug output"},{"location":"reference/cli/#exit-codes","title":"Exit Codes","text":"Code Meaning <code>0</code> The check completed and no breaking changes were found (or <code>--fail-on-bc</code> was not set) <code>1</code> <code>--fail-on-bc</code> was set and breaking changes were found <code>2</code> The command failed before producing a result"},{"location":"reference/maven-plugin/","title":"Maven plug-in options","text":"<p>This page is the exhaustive reference for the <code>roseau:check</code> goal configuration. The setup guide, baseline strategies, and report examples live in Maven plug-in. Most configurations only need a baseline plus an optional failure policy. The table below covers the full parameter set when a build requires more control.</p>"},{"location":"reference/maven-plugin/#goal","title":"Goal","text":"<ul> <li>Goal: <code>roseau:check</code></li> <li>Default phase: <code>verify</code></li> <li>Dependency resolution: <code>compile</code> scope</li> </ul> <p>Parameters with a Maven property can be overridden on the command line with <code>-D&lt;property&gt;=&lt;value&gt;</code>.</p>"},{"location":"reference/maven-plugin/#top-level-parameters","title":"Top-Level Parameters","text":"Parameter Property Type Default Description <code>skip</code> <code>roseau.skip</code> <code>boolean</code> <code>false</code> Skip plug-in execution <code>skipIfBaselineUnresolvable</code> <code>roseau.skipIfBaselineUnresolvable</code> <code>boolean</code> <code>false</code> Skip the check when the baseline artifact cannot be resolved from Maven repositories <code>binaryOnly</code> <code>roseau.binaryOnly</code> <code>boolean</code> false Report only binary-breaking changes <code>sourceOnly</code> <code>roseau.sourceOnly</code> <code>boolean</code> false Report only source-breaking changes <code>failOnIncompatibility</code> <code>roseau.failOnIncompatibility</code> <code>boolean</code> <code>false</code> Fail the build on any breaking change <code>failOnBinaryIncompatibility</code> <code>roseau.failOnBinaryIncompatibility</code> <code>boolean</code> <code>false</code> Fail the build on binary-breaking changes <code>failOnSourceIncompatibility</code> <code>roseau.failOnSourceIncompatibility</code> <code>boolean</code> <code>false</code> Fail the build on source-breaking changes <code>baselineCoordinates</code> <code>roseau.baselineCoordinates</code> <code>String</code> \u2014 Baseline as <code>groupId:artifactId:version[:extension[:classifier]]</code>; takes precedence over <code>baselineDependency</code> <code>baselineDependency</code> \u2014 <code>Dependency</code> \u2014 Baseline Maven coordinates as structured XML; use <code>baselineCoordinates</code> for CLI overrides <code>baselineJar</code> <code>roseau.baselineJar</code> <code>Path</code> \u2014 Path to a baseline JAR file <code>classpath</code> \u2014 <code>List&lt;Path&gt;</code> \u2014 Extra classpath entries shared by both versions <code>classpathPom</code> \u2014 <code>Path</code> \u2014 POM used to derive the shared classpath <code>baselineClasspath</code> \u2014 <code>List&lt;Path&gt;</code> \u2014 Extra classpath entries for the baseline only <code>baselineClasspathPom</code> \u2014 <code>Path</code> \u2014 POM used to derive the baseline classpath <code>reports</code> \u2014 <code>List&lt;ReportConfig&gt;</code> \u2014 Report files to generate <code>reportDirectory</code> <code>roseau.reportDirectory</code> <code>File</code> <code>${project.build.directory}/roseau</code> Output directory for relative report paths <code>exportBaselineApi</code> <code>roseau.exportBaselineApi</code> <code>Path</code> \u2014 Export the baseline API model as JSON <code>exportCurrentApi</code> <code>roseau.exportCurrentApi</code> <code>Path</code> \u2014 Export the current API model as JSON <code>configFile</code> <code>roseau.configFile</code> <code>Path</code> \u2014 Path to a <code>roseau.yaml</code> file <code>verbosity</code> <code>roseau.verbosity</code> <code>String</code> \u2014 Logging level: <code>QUIET</code>, <code>NORMAL</code>, <code>VERBOSE</code>, or <code>DEBUG</code>"},{"location":"reference/maven-plugin/#reports-entries","title":"<code>reports</code> Entries","text":"Field Type Required Description <code>file</code> <code>String</code> Yes Report file path; relative paths are resolved against <code>reportDirectory</code> <code>format</code> <code>String</code> Yes Report format: <code>CSV</code>, <code>HTML</code>, <code>JSON</code>, or <code>MD</code>"}]}